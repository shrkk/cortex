---
phase: 2
plan: "02-06"
type: execute
wave: 4
depends_on:
  - "02-04"
  - "02-03"
files_modified:
  - notch/NotchDrop/NotchDrop/Cortex/CortexClient.swift
  - notch/NotchDrop/NotchDrop/Cortex/CortexIngest.swift
  - notch/NotchDrop/NotchDrop/Cortex/CortexSettings.swift
  - notch/NotchDrop/NotchDrop/Cortex/CortexStatusView.swift
autonomous: true
agent: notch-specialist
requirements:
  - ING-01
  - ING-02
  - ING-03
  - ING-04
  - ING-05
  - ING-06
  - ING-07
  - ING-08
  - ING-09

must_haves:
  truths:
    - "CortexClient.swift compiles in Xcode with zero errors"
    - "CortexSettings.swift persists enabled and backendURL to UserDefaults"
    - "CortexIngest.swift checks image UTIs before file-url (UTI ordering)"
    - "CortexStatusView.swift renders a dismissible status pill"
    - "CortexClient includes matchCourse, fetchCourses, createCourse functions"
    - "Default backend URL is http://localhost:8000"
  artifacts:
    - path: "notch/NotchDrop/NotchDrop/Cortex/CortexClient.swift"
      provides: "Networking layer: sendFile, sendImage, sendURL, sendText, matchCourse, fetchCourses, createCourse"
      contains: "CortexClient\|matchCourse\|fetchCourses\|createCourse"
    - path: "notch/NotchDrop/NotchDrop/Cortex/CortexIngest.swift"
      provides: "NSItemProvider + NSPasteboard extraction with correct UTI ordering"
      contains: "UTType.image.identifier\|handleProviders\|handleClipboard"
    - path: "notch/NotchDrop/NotchDrop/Cortex/CortexSettings.swift"
      provides: "UserDefaults-backed settings"
      contains: "cortex.enabled\|cortex.backendURL"
    - path: "notch/NotchDrop/NotchDrop/Cortex/CortexStatusView.swift"
      provides: "SwiftUI status pill"
      contains: "CortexStatusView\|Sending to Cortex"
  key_links:
    - from: "CortexIngest.swift"
      to: "CortexClient.shared"
      via: "Task { await CortexClient.shared.sendX(...) }"
      pattern: "CortexClient.shared"
    - from: "CortexClient.swift"
      to: "/ingest endpoint"
      via: "URLRequest to CortexSettings.shared.backendURL + /ingest"
      pattern: "backendURL.*ingest"
---

<objective>
Create the four Cortex Swift files in `notch/NotchDrop/NotchDrop/Cortex/`. These files implement the networking layer, UTI-priority drag handler, UserDefaults settings, and status pill UI. Use the reference implementations from `.claude/agents/notch-specialist.md` verbatim — they have been tested and reviewed.

Purpose: These four files are the foundation of the notch integration. Plan 02-07 adds CortexCourseTab and the surgical edit on top of this foundation.
Output: Four Swift files in the Cortex/ subdirectory.
</objective>

<execution_context>
@/Users/shrey/Desktop/swe projects/cortex/cortex/.planning/phases/02-ingest-parsing-notch/02-06-PLAN.md
</execution_context>

<context>
@/Users/shrey/Desktop/swe projects/cortex/cortex/.planning/ROADMAP.md
@/Users/shrey/Desktop/swe projects/cortex/cortex/.planning/phases/02-ingest-parsing-notch/02-CONTEXT.md
@/Users/shrey/Desktop/swe projects/cortex/cortex/.planning/phases/02-ingest-parsing-notch/02-UI-SPEC.md
@/Users/shrey/Desktop/swe projects/cortex/cortex/.planning/phases/02-ingest-parsing-notch/02-04-SUMMARY.md
</context>

<interfaces>
<!-- Complete Swift reference implementations in .claude/agents/notch-specialist.md -->
<!-- The executor MUST read this file first — it contains verbatim Swift source for all 5 files -->
<!-- Key additions needed in CortexClient beyond the base template: -->
<!--
// Pre-flight course match
func matchCourse(hint: String) async -> Int?

// Fetch all courses
func fetchCourses() async -> [(id: Int, title: String)]

// Create a new course
func createCourse(title: String) async -> Int
-->

<!-- CortexStatusView UI spec from 02-UI-SPEC.md -->
<!--
States: idle (hidden), sending (accent #6366F1 85% opacity), success (systemGreen 15% + white border), error (systemRed 85%)
Pill: height 24pt, corner radius 12pt, horizontal padding 8pt, vertical 4pt
Font: 11pt semibold
Icons: SF Symbol arrow.up.circle (sending), checkmark.circle.fill (success), exclamationmark.circle.fill (error)
Entry: .spring(response:0.3, dampingFraction:0.7) slide-down + fade-in
Exit: .easeOut(duration:0.25) fade-out
-->

<!-- CortexSettings spec from 02-UI-SPEC.md -->
<!--
Controls:
  Toggle "Enable Cortex Drop" → UserDefaults key cortex.enabled (default: true)
  TextField "Backend URL" → UserDefaults key cortex.backendURL (default: http://localhost:8000)
Layout: VStack(alignment:.leading, spacing:16), 20pt outer padding
-->
</interfaces>

<tasks>

<task id="1" type="create" autonomous="true">
  <file>notch/NotchDrop/NotchDrop/Cortex/CortexSettings.swift</file>
  <action>
Create CortexSettings.swift using the verbatim reference implementation from `.claude/agents/notch-specialist.md`.

The file stores `enabled`, `backendURL`, and `courseId` in UserDefaults. The `courseId` property is kept for backward compatibility even though course is now dynamically assigned; it falls back to 1.

Key detail from 02-UI-SPEC.md: default `backendURL` = "http://localhost:8000" (D-13).

Use the exact implementation from the notch-specialist agent file — do not simplify or omit any properties.
  </action>
  <read_first>
    - /Users/shrey/Desktop/swe projects/cortex/cortex/.claude/agents/notch-specialist.md (MANDATORY — contains verbatim CortexSettings.swift source)
    - /Users/shrey/Desktop/swe projects/cortex/cortex/.planning/phases/02-ingest-parsing-notch/02-UI-SPEC.md (default values, UserDefaults keys)
  </read_first>
  <acceptance_criteria>
    - File exists at notch/NotchDrop/NotchDrop/Cortex/CortexSettings.swift
    - `grep "cortex.enabled\|cortex.backendURL\|cortex.courseId" notch/NotchDrop/NotchDrop/Cortex/CortexSettings.swift | wc -l` outputs `3`
    - `grep "localhost:8000" notch/NotchDrop/NotchDrop/Cortex/CortexSettings.swift` returns a match
    - `grep "ObservableObject\|@Published" notch/NotchDrop/NotchDrop/Cortex/CortexSettings.swift | wc -l` outputs at least `3`
  </acceptance_criteria>
</task>

<task id="2" type="create" autonomous="true">
  <file>notch/NotchDrop/NotchDrop/Cortex/CortexClient.swift</file>
  <action>
Create CortexClient.swift using the verbatim reference implementation from `.claude/agents/notch-specialist.md`, PLUS the three additional methods needed by CortexCourseTab (also documented in the notch-specialist agent):

1. `matchCourse(hint: String) async -> Int?` — calls `GET /courses/match?hint=...`
2. `fetchCourses() async -> [(id: Int, title: String)]` — calls `GET /courses`
3. `createCourse(title: String) async -> Int` — calls `POST /courses`

These three functions must be added to the `CortexClient` class alongside the base `sendFile`, `sendImage`, `sendURL`, `sendText` functions.

Also update `sendFile`, `sendImage`, `sendURL`, `sendText` to use the course resolution flow from the notch-specialist agent:
```swift
let hint = url.lastPathComponent  // or URL title, or first 200 chars of text
let courseId = await CortexCourseTabState.shared.sessionCourseId
    ?? (await CortexCourseTabState.shared.resolve(hint: hint))
    ?? 1  // fallback
```

Note: `CortexCourseTabState` is defined in CortexCourseTab.swift (Plan 02-07). Forward-reference is fine in Swift — both files will be in the same target.

Status auto-dismiss timers: success → 2s, error → 4s (per ING-07 and 02-UI-SPEC.md).
  </action>
  <read_first>
    - /Users/shrey/Desktop/swe projects/cortex/cortex/.claude/agents/notch-specialist.md (MANDATORY — CortexClient.swift full source + matchCourse/fetchCourses/createCourse additions)
    - /Users/shrey/Desktop/swe projects/cortex/cortex/.planning/phases/02-ingest-parsing-notch/02-UI-SPEC.md (status states, timing, copy strings)
  </read_first>
  <acceptance_criteria>
    - File exists at notch/NotchDrop/NotchDrop/Cortex/CortexClient.swift
    - `grep "func matchCourse\|func fetchCourses\|func createCourse" notch/NotchDrop/NotchDrop/Cortex/CortexClient.swift | wc -l` outputs `3`
    - `grep "sendFile\|sendImage\|sendURL\|sendText" notch/NotchDrop/NotchDrop/Cortex/CortexClient.swift | wc -l` outputs at least `4`
    - `grep "2_000_000_000\|4_000_000_000" notch/NotchDrop/NotchDrop/Cortex/CortexClient.swift | wc -l` outputs `2` (2s success, 4s error dismiss)
    - `grep "courses/match\|/courses" notch/NotchDrop/NotchDrop/Cortex/CortexClient.swift | wc -l` outputs at least `2`
  </acceptance_criteria>
</task>

<task id="3" type="create" autonomous="true">
  <file>notch/NotchDrop/NotchDrop/Cortex/CortexIngest.swift</file>
  <action>
Create CortexIngest.swift using the verbatim reference implementation from `.claude/agents/notch-specialist.md`.

CRITICAL UTI ordering (STATE.md pitfall #1 and ROADMAP stack note):
- Check `UTType.image.identifier` FIRST (before `public.file-url`)
- This prevents the browser image drag trap where `public.file-url` points to a temp file that disappears

The `handleProviders(_:)` function handles drag-and-drop.
The `handleClipboard()` function handles ⌘V paste (ING-04, ING-05, ING-06).
Clipboard priority: image → URL → string.

Use the exact implementation from the notch-specialist agent — it has already solved the UTI ordering issue.
  </action>
  <read_first>
    - /Users/shrey/Desktop/swe projects/cortex/cortex/.claude/agents/notch-specialist.md (MANDATORY — CortexIngest.swift full source with UTI ordering)
    - /Users/shrey/Desktop/swe projects/cortex/cortex/.planning/STATE.md (pitfall #1: UTI ordering)
  </read_first>
  <acceptance_criteria>
    - File exists at notch/NotchDrop/NotchDrop/Cortex/CortexIngest.swift
    - `grep "UTType.image.identifier" notch/NotchDrop/NotchDrop/Cortex/CortexIngest.swift` appears BEFORE `canLoadObject.*URL` in the file (UTI ordering)
    - `grep "handleProviders\|handleClipboard" notch/NotchDrop/NotchDrop/Cortex/CortexIngest.swift | wc -l` outputs `2`
    - `grep "NSPasteboard.general\|NSImage(pasteboard" notch/NotchDrop/NotchDrop/Cortex/CortexIngest.swift` confirms clipboard handling
  </acceptance_criteria>
</task>

<task id="4" type="create" autonomous="true">
  <file>notch/NotchDrop/NotchDrop/Cortex/CortexStatusView.swift</file>
  <action>
Create CortexStatusView.swift. Use the reference implementation from the notch-specialist agent as a base, then apply the UI-SPEC.md refinements:

From 02-UI-SPEC.md:
- Sending state: background `Color(red: 0.388, green: 0.400, blue: 0.945).opacity(0.85)` (Cortex Indigo)
- Success state: `Color(nsColor: .systemGreen).opacity(0.15)` + white border 0.5pt
- Error state: `Color(nsColor: .systemRed).opacity(0.85)`
- Icons: `arrow.up.circle` (sending), `checkmark.circle.fill` (success), `exclamationmark.circle.fill` (error)
- Font: `.font(.system(size: 11, weight: .semibold))`
- Height: 24pt, corner radius: 12pt (Capsule)
- Entry animation: `.spring(response: 0.3, dampingFraction: 0.7)`
- Exit animation: `.easeOut(duration: 0.25)`
- Accessibility: `.accessibilityLabel("Cortex status: \(stateLabel)")`
- Reduced motion: check `NSAccessibility.isReduceMotionEnabled`; if true, use `.animation(nil)`

Copy strings (from 02-UI-SPEC.md):
- Sending: "Sending to Cortex…"
- Success: "Sent to [Course Name]" (dynamic from CortexClient.status.message)
- Error: "Upload failed — check settings"
  </action>
  <read_first>
    - /Users/shrey/Desktop/swe projects/cortex/cortex/.claude/agents/notch-specialist.md (CortexStatusView.swift base implementation)
    - /Users/shrey/Desktop/swe projects/cortex/cortex/.planning/phases/02-ingest-parsing-notch/02-UI-SPEC.md (component spec: pill states, colors, animations, copy strings)
  </read_first>
  <acceptance_criteria>
    - File exists at notch/NotchDrop/NotchDrop/Cortex/CortexStatusView.swift
    - `grep "Sending to Cortex\|Upload failed" notch/NotchDrop/NotchDrop/Cortex/CortexStatusView.swift | wc -l` outputs `2`
    - `grep "arrow.up.circle\|checkmark.circle.fill\|exclamationmark.circle.fill" notch/NotchDrop/NotchDrop/Cortex/CortexStatusView.swift | wc -l` outputs `3`
    - `grep "spring\|easeOut" notch/NotchDrop/NotchDrop/Cortex/CortexStatusView.swift | wc -l` outputs `2`
    - `grep "accessibilityLabel\|isReduceMotionEnabled" notch/NotchDrop/NotchDrop/Cortex/CortexStatusView.swift | wc -l` outputs `2`
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Swift app → backend API | URLSession calls to localhost:8000; user can change URL in settings |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-06-01 | Spoofing | backendURL editable in settings | accept | Local dev only; no credentials stored; user controls their own localhost; no production URL exposure risk |
| T-02-06-02 | Information Disclosure | CortexSettings UserDefaults | accept | Stores only URL and enabled flag; no secrets or PII in UserDefaults |
| T-02-06-03 | Denial of Service | image drag temp file | mitigate | UTI ordering fix: image UTI checked before file-url prevents reading vanished temp files; CortexIngest.swift already handles this |
| T-02-06-04 | Elevation of Privilege | NSEvent global keyboard monitor | mitigate | Accessibility permission checked at startup via AXIsProcessTrustedWithOptions; monitor is nil if permission denied; handled in Plan 02-07 |
</threat_model>

<verification>
```bash
# Verify all four files exist
ls /Users/shrey/Desktop/swe\ projects/cortex/cortex/notch/NotchDrop/NotchDrop/Cortex/

# Verify UTI ordering in CortexIngest.swift
grep -n "UTType.image\|canLoadObject.*URL" \
  /Users/shrey/Desktop/swe\ projects/cortex/cortex/notch/NotchDrop/NotchDrop/Cortex/CortexIngest.swift
# image line number MUST be less than URL line number

# Verify course resolution methods
grep "func matchCourse\|func fetchCourses\|func createCourse" \
  /Users/shrey/Desktop/swe\ projects/cortex/cortex/notch/NotchDrop/NotchDrop/Cortex/CortexClient.swift

# Verify status copy strings
grep "Sending to Cortex\|Upload failed" \
  /Users/shrey/Desktop/swe\ projects/cortex/cortex/notch/NotchDrop/NotchDrop/Cortex/CortexStatusView.swift
```

Build verification: Open `notch/NotchDrop/NotchDrop.xcodeproj` in Xcode, add all four Swift files to the NotchDrop target, and do Product → Build (⌘B). Zero errors expected.
</verification>

<success_criteria>
- All four files exist in notch/NotchDrop/NotchDrop/Cortex/
- UTI ordering: image checked before file-url in CortexIngest.swift
- CortexClient has matchCourse, fetchCourses, createCourse
- CortexStatusView uses correct copy strings from UI-SPEC
- Xcode build produces zero errors (checkpoint verification in Plan 02-07)
</success_criteria>

<output>
After completion, create `.planning/phases/02-ingest-parsing-notch/02-06-SUMMARY.md`
</output>
